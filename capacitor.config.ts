import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-react-modal-issue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
